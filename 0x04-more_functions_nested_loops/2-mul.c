#include "main.h"

/**
 * mul - function to multiply two integers.
 *
 * @a: first character to be multiplied
 *
 * @b: second charactr to be mltiplied
 *
 * Return: 0 Always
 */
int mul(int a, int b)
{
	return (a * b);
}

