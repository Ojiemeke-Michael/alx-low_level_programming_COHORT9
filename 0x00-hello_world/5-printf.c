#include <stdio.h>

/**
 * main - Entry point
 *Description - to print strings of words to the standard output
 *Return: Always 0 (Success)
 */
int main(void)
{
	/*the command printf prints strings inputed into it*/
	printf("with proper grammar, but the outcome
		is a piece of art,\n");
	return (0);
